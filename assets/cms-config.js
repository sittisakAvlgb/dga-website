/* =====================================================================
   cms-config.js — ตั้งค่าการเชื่อมต่อ Strapi CMS
   • เว้นว่าง ('')  = ปิด CMS → ใช้ข้อมูลสำรอง (news-data.js / jobs-data.js)
   • ใส่ URL ของ Strapi เพื่อดึงเนื้อหาสด เช่น 'http://localhost:1337'
     หรือโดเมน production เช่น 'https://cms.dga.or.th'
   ===================================================================== */
window.DGA_CMS_URL = window.DGA_CMS_URL || '';
